diff --git a/dist/index.js b/dist/index.js
--- a/dist/index.js
+++ b/dist/index.js
@@ -14,8 +14,8 @@ export function HstNuxt() {
     return {
         name: '@histoire/plugin-nuxt',
         async defaultConfig() {
             const nuxtViteConfig = await useNuxtViteConfig();
-            const { viteConfig } = nuxtViteConfig;
+            const { css, viteConfig } = nuxtViteConfig;
             nuxt = nuxtViteConfig.nuxt; // We save it to close it later
             const plugins = viteConfig.plugins.filter((p) => !ignorePlugins.includes(p?.name));
             return {
@@ -64,7 +64,11 @@ export function HstNuxt() {
                     logLevel: 'info',
                 },
                 setupCode: [
-                    `${nuxt.options.css.map(file => `import '${file}'`).join('\n')}`,
+                    `export async function setupVue3 (payload) {
+  if (window.self !== window.top || (payload.variant && payload.story?.layout?.iframe !== true)) {
+${css.map(file => `    await import('${file}')`).join('\n')}
+  }
+}`,
                     `import { setupNuxtApp } from '@histoire/plugin-nuxt/dist/runtime/app-setup.js'
 export async function setupVue3 () {
   await setupNuxtApp(${JSON.stringify(nuxt.options.runtimeConfig.public)})
@@ -137,9 +141,13 @@ async function useNuxtViteConfig() {
         presets.push({
             from: '#build/histoire/composables.mjs',
             imports: stubbedComposables,
         });
     });
+    const css = [];
     return {
+        css,
         viteConfig: await new Promise((resolve, reject) => {
             nuxt.hook('modules:done', () => {
+                css.push(...nuxt.options.css);
+                nuxt.options.css = [];
                 nuxt.hook('components:extend', (components) => {
diff --git a/src/index.ts b/src/index.ts
--- a/src/index.ts
+++ b/src/index.ts
@@ -20,7 +20,7 @@ export function HstNuxt(): Plugin {
 
     async defaultConfig() {
       const nuxtViteConfig = await useNuxtViteConfig()
-      const { viteConfig } = nuxtViteConfig
+      const { css, viteConfig } = nuxtViteConfig
 
       nuxt = nuxtViteConfig.nuxt // We save it to close it later
       const plugins = viteConfig.plugins.filter((p: any) => !ignorePlugins.includes(p?.name))
@@ -71,7 +71,11 @@ export function HstNuxt(): Plugin {
           logLevel: 'info',
         },
         setupCode: [
-          `${nuxt.options.css.map(file => `import '${file}'`).join('\n')}`,
+          `export async function setupVue3 (payload) {
+  if (window.self !== window.top || (payload.variant && payload.story?.layout?.iframe !== true)) {
+${css.map(file => `    await import('${file}')`).join('\n')}
+  }
+}`,
           `import { setupNuxtApp } from '@histoire/plugin-nuxt/dist/runtime/app-setup.js'
 export async function setupVue3 () {
   await setupNuxtApp(${JSON.stringify(nuxt.options.runtimeConfig.public)})
@@ -157,8 +161,12 @@ async function useNuxtViteConfig() {
       imports: stubbedComposables,
     })
   })
 
+  const css: Nuxt['options']['css'] = []
   return {
+    css,
     viteConfig: await new Promise<ViteConfig>((resolve, reject) => {
       nuxt.hook('modules:done', () => {
+        css.push(...nuxt.options.css)
+        nuxt.options.css = []
         nuxt.hook('components:extend', (components) => {
